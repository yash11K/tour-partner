import { PermissionsGuardGuard } from './permissions-guard.guard';

describe('PermissionsGuardGuard', () => {
  it('should be defined', () => {
    expect(new PermissionsGuardGuard()).toBeDefined();
  });
});
